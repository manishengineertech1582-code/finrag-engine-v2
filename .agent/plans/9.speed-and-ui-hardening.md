# 9. speed-and-ui-hardening.md

## Goal
Reduce compound-query latency, cut avoidable token usage, and clean the old repo chat/documents UI without changing projects.

## Tasks
1. Optimize compound-query execution to avoid one full generation pass per clause.
2. Reduce prompt/context size with a smaller per-clause retrieval budget and concise generation prompts.
3. Add response metadata so the chat UI can explain focused multi-part handling cleanly.
4. Remove artificial post-response UI lag from the chat window.
5. Replace the global OCR danger banner with a smarter, dismissible note that appears only when relevant to PDF usage.
6. Add regression coverage and run backend + frontend validation in `C:\Users\manis\Downloads\Production-Project\finrag-engine-old`.

## Validation
- `./.venv/Scripts/pytest.exe tests/test_multiquery_multiintent.py tests/test_latency_hardening.py tests/test_response_schema.py -q`
- `npm run build` in `frontend/`
