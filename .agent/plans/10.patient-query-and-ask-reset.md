# 10. patient-query-and-ask-reset.md

## Goal
Fix incomplete patient-query answers and clear the Ask composer after successful queries in the live old repo.

## Tasks
1. Expand patient-query normalization so `patent info for Males` resolves to the intended patient query.
2. Split coordinated patient comparison prompts like `male ... and female ...` into focused clauses.
3. Clear the Ask window after successful query completion without wiping it on failure.
4. Add regression coverage for the new backend parsing rules.
5. Validate backend tests and frontend build, then update `PROGRESS.md`.

## Validation
- `./.venv/Scripts/pytest.exe tests/test_multiquery_multiintent.py tests/test_api.py -q`
- `npm run build` in `frontend/`
