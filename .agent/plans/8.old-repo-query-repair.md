# 8. old-repo-query-repair.md

## Goal
Permanently fix mixed-intent query failures in `C:\Users\manis\Downloads\Production-Project\finrag-engine-old` so the live FastAPI app no longer sends blended patient + concept prompts through a single MultiQuery retrieval call.

## Tasks
1. Inspect the current query pipeline and existing test coverage in the old repo.
2. Strengthen compound-query detection beyond `2+ ?` and a tiny connector list.
3. Split mixed prompts into standalone clauses and answer each clause independently.
4. Normalize obvious patient-query typo patterns that are currently poisoning retrieval.
5. Aggregate answers and citations back into the existing `QueryResponse` contract.
6. Add regression tests for the reported prompt pattern and for clause detection/splitting.
7. Run targeted backend validation in the old repo and record the outcome in `PROGRESS.md`.

## Validation
- `pytest tests/test_multiquery_multiintent.py tests/test_api.py -q`
- Add any narrower targeted test invocations if needed during iteration.
