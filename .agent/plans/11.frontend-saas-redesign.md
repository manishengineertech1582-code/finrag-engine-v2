# 11. frontend-saas-redesign.md

## Goal
Transform the existing React app in `C:\Users\manis\Downloads\Production-Project\finrag-engine-old` into a production-grade, investor-demo-ready SaaS UI while keeping the query flow cost-aware and performant.

## Tasks
1. Establish a shared dark design system and upgrade the core UI primitives.
2. Redesign the fixed sidebar, top bar, and overall app layout.
3. Rebuild Dashboard and Documents surfaces with stronger hierarchy, better empty/loading states, and smoother interactions.
4. Deliver a Perplexity-style Query page with centered input, rich answer card, inline citations, hover previews, confidence meter, and improved sources UX.
5. Add token-load visibility in the top bar using a real estimate from query history.
6. Validate with `npm run build` and any necessary backend/frontend checks, then update `PROGRESS.md`.

## Validation
- `npm run build` in `frontend/`
- Any targeted backend tests if the redesign requires additional response metadata or query behavior changes.
