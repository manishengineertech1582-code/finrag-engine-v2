[project]
name = "finrag-engine-v2"
version = "2.0.0"
description = "Production-grade RAG platform for PDF, DOCX, Excel, CSV, and TXT documents"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []
